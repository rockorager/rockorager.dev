baseURL = 'https://rockorager.dev/'
languageCode = 'en-us'
title = 'rockorager.dev'

[params]
description = 'A minimal blog'

[markup]
  [markup.highlight]
    noClasses = false
    guessSyntax = true
    lineNos = false
    tabWidth = 4
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true
        title = true
